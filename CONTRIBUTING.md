# 参与维护

欢迎提 issue 和 PR。这份文档讲三件事：**代码在哪、有哪几条硬规矩、怎么加东西**。

---

## 一、先跑起来

```bash
git clone https://github.com/TbusOS/claude-egress-monitor.git
cd claude-egress-monitor

python3 -m cem doctor                            # 不联网，看本机三条路径
python3 -m unittest discover -s tests -t . -v    # 全部离线，应该全绿
python3 -m cem serve --demo --open               # 用虚构数据看界面
```

需要 Python 3.9+，**不装任何第三方包**。这一条是设计约束，不是暂时状态 ——
一个"检查你网络"的工具，依赖越少越容易被信任。加依赖的 PR 需要在描述里
说明为什么标准库做不到。

---

## 二、模块地图

按"数据从哪来 → 怎么算 → 怎么显示"分层。改东西之前先看这张表，
找到那一层再动手。

### 采集层（跟外界打交道）

| 文件 | 管什么 |
|---|---|
| `net.py` | 带分段计时的 HTTPS 客户端。直连和 HTTP `CONNECT` 两条路径都在这 |
| `paths.py` | **三个入口各走哪条网络路径**。整个工具的核心概念 |
| `resolve.py` | 本机解析 vs 公网权威解析的对账；fake-ip / 私有地址 / CGNAT 的判定 |
| `sockets.py` | 用 `lsof` 列 Claude 进程此刻连着谁 |
| `clash.py` | 可选：读本地分流器的控制接口，补上 `lsof` 看不到的那一半 |
| `asn.py` | IP 归属：Team Cymru DNS + ip-api + rDNS |
| `cloudranges.py` | 厂商官方 IP 段（AWS / GCP / Cloudflare / DO / Oracle） |
| `rdap.py` | RDAP 注册信息，走 IANA bootstrap 定位该问哪个 RIR |
| `pathtrace.py` | `mtr` 包装：逐跳延迟与丢包。没装就降级 |
| `telemetry.py` | 从本机的 Claude Code 里**静态提取**遥测字段 |

### 计算层（不碰网络，纯函数为主）

| 文件 | 管什么 |
|---|---|
| `model.py` | 全部数据结构。**不可变**（frozen dataclass） |
| `probe.py` | 把各个探测拼成一轮完整采样 |
| `diagnose.py` | 出口画像、稳定性、「现象 → 判据 → 成因 → 方案」的诊断规则 |
| `checks.py` | 环境级检查（时钟、证书、IPv6、DNS 交叉验证……） |
| `endpoints.py` | Claude 会连的域名清单 + 每一行的取证来源 |
| `store.py` / `history.py` | 内存环形缓冲 / 按天归档、汇总、删除 |

### 呈现层

| 文件 | 管什么 |
|---|---|
| `view.py` | 把采样结果整形成界面要的形状。**只出事实，不出文案** |
| `server.py` | 本地 HTTP + SSE 推送 |
| `sampler.py` | 后台采样循环和开关 |
| `cli.py` | 命令行入口 |
| `web/index.html` · `web/app.js` | 界面。双语文案写在这里 |
| `demo.py` | 一轮完全虚构的采样，用来出截图和离线调界面 |

---

## 三、四条硬规矩

这四条不是风格偏好，是这个工具能不能被信任的前提。

### 1. 仓库里不能有任何人的本机网络信息

出口 IP、代理端口、内网地址、tailnet 名、运营商名、城市 —— **一律不进仓库**，
包括测试样本和代码注释里。这两个地方最容易漏（本仓在一次提交前扫描里
真的抓到过）。

演示数据和文档一律用文档保留段，它们永久保留给文档使用，
不可能被误当成真实数据：

```
192.0.2.0/24     198.51.100.0/24     203.0.113.0/24     2001:db8::/32
```

**例外**：目标方的公开基础设施地址（`api.anthropic.com` 解析到的地址、
Datadog intake 的地址）不是本机信息 —— 那正是这类仓库要记录的事实，可以留。

需要针对自己的机器实测时，另建一个**仓库外的目录**，实测输出、截图、
日志全落那里。

### 2. 不确定的结论必须标成推测

每个结论都要能看出**它有多硬**。分四档，落在 `model.py` 的 `AsnInfo`：

| 档位 | 判据 |
|---|---|
| **确证** | 权威来源直接说的（厂商自己发布的 IP 段） |
| **较可能** | 第三方数据源的判断 |
| **推测** | 单一弱线索（组织名关键词之类） |
| **判不出** | 没线索 |

**判不出就写判不出，不猜。** 尤其当猜错的方向不对称时 ——
把机房 IP 猜成家宽会让人以为风险更低，那是最危险的方向。

数据源给不了的维度不要假装能给：免费 IP 情报只有 `hosting` 一个布尔值，
区分不了住宅和企业，所以那一档叫「非机房宽带」而不是「住宅」。

### 3. 界面不发明数字

`web/app.js` 只渲染服务端返回的字段。没有的值渲染成破折号，
**绝不用 0、"未知"或上一轮的值顶上** —— 一个假的 0 会被读成"延迟 0 毫秒"。

同理，服务端拿不到的数据宁可返回 `null`。`pathtrace.py` 里有个现成例子：
`mtr` 对没回包的跳填 `0.0`，原样透出去就成了"往返 0 毫秒"，
所以那里显式改成了 `None`。

### 4. 结论只算 Claude 的域名

`1.1.1.1` 在域名清单里，但它是**对照组**，不属于 Claude。
把它算进"Claude 域名出口一致性"会产生假警报，引导人去改一份没问题的配置。

所有结论计算必须走 `probe.claude_traces()` 过滤，不要直接遍历
`sample.traces`。`endpoints.py` 里的 `baseline: bool` 就是干这个的。

---

## 四、怎么加东西

### 加一个 Claude 域名

改 `cem/endpoints.py`。每一行都要带 `evidence`，说明这个域名是怎么知道的：

| evidence | 意思 |
|---|---|
| `cli-strings` | 从 Claude Code 单文件可执行里抽出的字符串 |
| `desktop-asar` | 从桌面端 `app.asar` 里抽出的 |
| `observed` | 本机 `lsof` 真实抓到过 |
| `docs` | 官方文档写了 |

复现命令写在 [docs/01-endpoints.md](docs/01-endpoints.md)，加域名时请一并
更新那份文档，让别人能自己跑一遍对账。

不是 Claude 的域名（对照组、公共解析器）必须标 `baseline=True`。

### 加一个数据源

放进采集层，接口对齐现有的：**拿不到就返回 None，不要抛异常**。
一个可选增强不该让主流程失败。

新数据源要在 [docs/07-datasources.md](docs/07-datasources.md) 里加一行，
写清楚：地址、给什么、更新频率、缓存多久、什么许可、**能撑到哪一档置信度**。

缓存 TTL 要短，并且把拉取时间显示在界面上。拉一次用一年比没有缓存更危险 ——
它会给出一个看起来很确定的过期答案。

### 加一条诊断规则

改 `cem/diagnose.py`。每条 `Finding` 必须有四样：

```python
Finding(
    id="...",
    severity="warn",
    title="现象（一句话，别复述数据）",
    evidence="判据 —— 算出它的那几个实测数字",
    cause="成因 —— 不是现象的复述",
    fix="下一步 —— 可以直接照做的命令或配置",
    docs="docs/03-routing.md",
)
```

**没有判据的建议不值得照做。** 只写"出口不一致"而不写"哪几个域名、
分别落在哪"，读者没法验证，也没法动手。

写完加一条测试，喂一份构造的 Sample，断言这条规则**在该触发时触发、
不该触发时不触发**。第二半更重要 —— 假警报比漏报更消耗信任。

### 改界面

- 组件用 `atl-` 前缀的是 [atelier-design](web/assets/NOTICE.md) 那套 skill 的，
  别改；页面自己的样式一律**无前缀**（`.surface-card`、`.hop-row` 之类）
- 所有节点用 `el()` 造，文本走 `textContent`。界面显示的是主机名、进程名、
  ASN 组织名，都是从系统里读来的外部输入，**拼进 innerHTML 就是一个 XSS**
- 文案双语，用 `bi(zh, en)`。JS 字符串里不要写 `**粗体**`，
  那是 Markdown 标记，在页面上会原样显示出来

### 改文档

文档源是 `docs/*.md`。改完跑一次生成器，把生成的 `.html` 一起提交：

```bash
python3 scripts/build-docs.py
```

生成器只用标准库。这样 GitHub Pages 不需要任何 CI 就能更新。

---

## 五、测试

```bash
python3 -m unittest discover -s tests -t . -v
```

约定：

- **全部离线。** 解析器写成纯函数，喂固定样本。没有一个测试会发请求 ——
  一个需要联网才能跑的测试套件，在没网的时候就等于没有
- 解析类的改动必须带样本。样本里的地址用文档保留段
- 修 bug 时**先写一个能复现的失败测试**，再修

---

## 六、提交

```
<type>: <一句话说清楚做了什么>

<可选正文：为什么这么做，以及不这么做会怎样>
```

`type` 取 `feat` / `fix` / `refactor` / `docs` / `test` / `chore` / `perf` / `ci`。

提交前自己过一遍：

- [ ] 测试全绿
- [ ] 没有本机网络信息（出口 IP、代理端口、运营商名、城市、`/Users/<用户名>`）
- [ ] 新结论标了置信度
- [ ] 新数据源写进了 `docs/07-datasources.md`
- [ ] 改了 `.md` 的话跑过 `scripts/build-docs.py`

---

## 七、语言

代码注释和文档写**白话 + 精确技术术语**。

注释回答"**为什么**"，不复述"是什么"。这个仓里最有价值的注释都是
在讲一个坑：为什么 ALPN 只能申报 `http/1.1`、为什么 RDAP 要先读 IANA
引导文件、为什么 `atl-meter` 放进 grid 要显式转块。这些东西不写下来，
下一个人会再踩一遍。

中英文都欢迎。已有文档是中文的，PR 用中文或英文都行。
